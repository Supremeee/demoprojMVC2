@model demoprojMVC.UI.Portal.Models.RoleInfoViewModel

@{
    ViewBag.Title = "角色信息管理";
    AjaxOptions ajaxOpts = new AjaxOptions
    {
        UpdateTargetId = "roleInfo",
        Url = Url.Action("GetRoleInfoData")
    };
}
<div class="panel panel-default">
    <div class="panel-heading">
        <h3 class="text-center">角色信息管理</h3>
        @if (TempData["Message"] != null)
        {
            <div class="alert alert-success">@TempData["Message"]</div>
        }
        <div class="form-group form-inline container">
            @using (Ajax.BeginForm(ajaxOpts))
            {
                <div class="col-sm-2">
                    @Html.ActionLink("新增", "Create", null, new { @class = "btn btn-primary btn-sm" })
                </div>
                <div class="col-sm-8 col-md-4">
                    <label>查询条件:</label><input type="text" id="searchContext" name="searchContext" class="form-control input-sm" style="width: 120px;" />
                </div>
                <div class="col-sm-10 col-md-6">
                    <label>查询方式:</label>
                    <label class="radio-inline">
                        <input type="radio" name="type" checked="checked" value="RoleName"> 角色名
                    </label>
                    <label class="radio-inline">
                        <input type="radio" name="type" value="Url"> 备注
                    </label>
                    <input type="submit" class="btn btn-primary btn-sm" value="查询" />
                </div>
            }
        </div>
    </div>
    <div id="roleInfo" class="panel-body">
        @Html.Action("GetRoleInfoData")
    </div>

</div>



